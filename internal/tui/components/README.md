# Components

This package mainly deals with providing shareable components to the UI.
This will contain both universal workspace components aswell as page specifics.

Essentially every folder here is a model. In bubble tea, all components are bubbletea
models itself, which can process, change state, acts according and controlled by parent model.
